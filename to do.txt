pong to do

definitely do
-----------------------

the bounce count font looks a little ass, seems blurry around the edges
	probably scale, look into changing font size tween for real this time - font overrides, theme overrides, themeoverride.fontsize

make the vs computer input style be changeable
	click on keyboard icon to make it a cursor, and vice versa. popup on load in? if moved cursor a lot recently, pop up again? want to make sure you can see it again if you miss the popup - probably solved by watching playtesters that aren't prompted

-----------------------


have the very first ball drop in main take longer (to let player mess with controls)

the button hitbox is the 32x32 square, not the button sprite shape

the bounce high score doesn't seem obviously like that, maybe it seems like a goal

























































would be nice
-----------------------
ai: try to get some sort of variation so the main menu looks for dynamic, some sort of reaction speed based movement - or maybe some +- error in its angle calculations? maybe do some smarter raycast stuff https://www.youtube.com/watch?v=tjSuQzbarAQ?
currently i like how to beat the ai you have to think ahead, since it primitively sees where the ball will be. i do like the idea of different ai patterns/styles/presets that have their own flair. maybe 3 with scaling levels of difficulty?
	maybe one perfect (for solo bounce count high scores), one accessible (like it is now) for reasonable competitive challenge, and one human-like with a name? try to beat all the characters
	to make the ai feel reactive, have it explicitly react. call the movement changes on ball bounce? or at least on paddle bounce
	currently i'm trying to get the ball to exactly the top/bottom 75/25% of the paddle - randomize this so it's less robotic

new sprites?

wall -> build corner, build small wall segment that's good to tile. look into 2d z-fighting so i know if the ball will show over the wall
make little tiled background for outside the arena, thinking little star-like twinkles or something
	do this with a perlin noise type background

some extra polish/juice all around, sound effects? tiny bit of camera shake? tiny bit of paddle movement on hit?
	instead of ball acceleration, whole game acceleration? paddles move faster, animate faster?

improve high speed collisions: (in ball process - before moving from position to position(delta), send out a raycast and see if there's any collision in the line between position and position(delta). if so, move and then ensure collision signal is sent) (test by removing ball tilt and yeeting balls into paddles at untenable speeds)
or just restructure to rigidbody/character controllers
or elsewise

raycast: length = velocity.x

ball: on process check if raycast in collider, if so collider=off for 0.2s, rebound
theoretically if the ball moves velocity, then a raycast of length velocity will always work?


raycast psuedo code:
instead of position += velocity
var potential_end_point = position + velocity
var abort = false
while position != potential_end_point && not abort:
	position += velocity.direction
	if check_collisions():
		abort = true

set_velocity(new_velocity):
	velocity = new_velocity
	abort = false
	
-----------------------